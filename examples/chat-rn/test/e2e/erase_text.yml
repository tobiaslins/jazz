appId: com.jazz.chatrn
---
- copyTextFrom:
   id: ${id}
- repeat:
    times: 4
    # while:
    #   true: ${maestro.copiedText.length > 0}
    #   notVisible: "Paste"
    commands:
      - tapOn:
          id: ${id}
      - eraseText
      - copyTextFrom:
          id: ${id}
